# Keep the Docker build context tight — only package.json files + source + lockfile
# are copied explicitly by the Dockerfile. Everything else is excluded.
**/node_modules
**/dist
**/.turbo
**/coverage
**/*.log
**/.env
**/.env.*
.git
.github
*.md
**/tsconfig.tsbuildinfo
